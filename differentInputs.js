const compressBoxesTwice = (boxes1, boxes2) => {
	// two different inputs
	boxes1.forEach((element) => {
		console.log(element);
	});

	boxes2.forEach((element) => {
		console.log(element);
	});
};

// O(a + b) - where boxes1.length = a & boxes2.length = b
